<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Allah Knows Us - Firebase Auth</title>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-auth-compat.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            background-color: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(to right, #3a7bd5, #00d2ff);
            color: white;
            padding: 25px;
            text-align: center;
            position: relative;
        }
        
        .header h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }
        
        .header p {
            font-size: 16px;
            opacity: 0.9;
        }
        
        .content {
            padding: 25px;
        }
        
        .section {
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #eee;
        }
        
        .section:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        h2 {
            color: #2c3e50;
            margin-bottom: 15px;
            font-size: 22px;
            display: flex;
            align-items: center;
        }
        
        h2 i {
            margin-right: 10px;
            color: #3a7bd5;
        }
        
        .config-item {
            margin-bottom: 15px;
        }
        
        .config-item strong {
            display: block;
            color: #34495e;
            margin-bottom: 5px;
        }
        
        .config-item span {
            display: inline-block;
            background-color: #f8f9fa;
            padding: 8px 12px;
            border-radius: 5px;
            font-family: monospace;
            word-break: break-all;
            font-size: 14px;
            border: 1px solid #e9ecef;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            color: #34495e;
            font-weight: 500;
        }
        
        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            transition: border-color 0.3s;
        }
        
        input[type="email"]:focus, input[type="password"]:focus {
            border-color: #3a7bd5;
            outline: none;
            box-shadow: 0 0 0 3px rgba(58, 123, 213, 0.2);
        }
        
        .btn {
            display: inline-block;
            padding: 12px 20px;
            background-color: #3a7bd5;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 500;
            transition: all 0.3s;
            margin-right: 10px;
            margin-bottom: 10px;
        }
        
        .btn:hover {
            background-color: #2c5eaa;
            transform: translateY(-2px);
        }
        
        .btn-success {
            background-color: #28a745;
        }
        
        .btn-success:hover {
            background-color: #218838;
        }
        
        .btn-danger {
            background-color: #dc3545;
        }
        
        .btn-danger:hover {
            background-color: #c82333;
        }
        
        .status {
            padding: 12px;
            border-radius: 5px;
            margin-top: 20px;
            text-align: center;
            font-weight: 500;
        }
        
        .success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        
        .error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        
        .user-info {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
            border: 1px solid #e9ecef;
        }
        
        .user-info p {
            margin-bottom: 10px;
            word-break: break-all;
        }
        
        .hidden {
            display: none;
        }
        
        .footer {
            text-align: center;
            padding: 20px;
            color: #6c757d;
            font-size: 14px;
            background-color: #f8f9fa;
            border-top: 1px solid #e9ecef;
        }
        
        .islamic-pattern {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 10px;
            background: repeating-linear-gradient(
                45deg,
                rgba(255,255,255,0.1),
                rgba(255,255,255,0.1) 10px,
                transparent 10px,
                transparent 20px
            );
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Allah Knows Us</h1>
            <p>Firebase Authentication Interface</p>
            <div class="islamic-pattern"></div>
        </div>
        
        <div class="content">
            <div class="section">
                <h2><i>⚙️</i> Firebase Configuration</h2>
                <div class="config-item">
                    <strong>API Key</strong>
                    <span>AizrSyBZKEdop3uOprqKAWDIOUctp19 - h4B34</span>
                </div>
                <div class="config-item">
                    <strong>Auth Domain</strong>
                    <span>allah-knows-us.firebaseapp.com</span>
                </div>
                <div class="config-item">
                    <strong>Project ID</strong>
                    <span>allah-knows-us</span>
                </div>
                <div class="config-item">
                    <strong>Storage Bucket</strong>
                    <span>allah-knows-us.firebasestorage.app</span>
                </div>
                <div class="config-item">
                    <strong>Messaging Sender ID</strong>
                    <span>739942116421</span>
                </div>
                <div class="config-item">
                    <strong>App ID</strong>
                    <span>1731942116421webxclccf3c45398cbcdoc172c</span>
                </div>
                <button id="initFirebase" class="btn">Initialize Firebase</button>
                <div id="initStatus" class="status hidden"></div>
            </div>
            
            <div class="section">
                <h2><i>🔐</i> Authentication Test</h2>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" value="shifatkhan@gmail.com">
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" placeholder="Enter password">
                </div>
                <button id="register" class="btn">Register Test User</button>
                <button id="login" class="btn btn-success">Login</button>
                <div id="authStatus" class="status hidden"></div>
            </div>
            
            <div class="section">
                <h2><i>👤</i> User Information</h2>
                <div id="userInfo" class="user-info hidden">
                    <p><strong>Email:</strong> <span id="userEmail"></span></p>
                    <p><strong>User ID:</strong> <span id="userId"></span></p>
                    <button id="logout" class="btn btn-danger">Logout</button>
                </div>
                <div id="noUserInfo" class="status">
                    No user is currently logged in.
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>Firebase Authentication Demo for "Allah Knows Us"</p>
        </div>
    </div>

    <script>
        // Firebase configuration
        const firebaseConfig = {
            apiKey: "AizrSyBZKEdop3uOprqKAWDIOUctp19 - h4B34",
            authDomain: "allah-knows-us.firebaseapp.com",
            projectId: "allah-knows-us",
            storageBucket: "allah-knows-us.firebasestorage.app",
            messagingSenderId: "739942116421",
            appId: "1731942116421webxclccf3c45398cbcdoc172c"
        };
        
        // DOM elements
        const initFirebaseBtn = document.getElementById('initFirebase');
        const initStatus = document.getElementById('initStatus');
        const registerBtn = document.getElementById('register');
        const loginBtn = document.getElementById('login');
        const authStatus = document.getElementById('authStatus');
        const logoutBtn = document.getElementById('logout');
        const userInfo = document.getElementById('userInfo');
        const noUserInfo = document.getElementById('noUserInfo');
        const userEmail = document.getElementById('userEmail');
        const userId = document.getElementById('userId');
        
        // Initialize Firebase
        let auth = null;
        
        initFirebaseBtn.addEventListener('click', () => {
            try {
                // Initialize Firebase
                firebase.initializeApp(firebaseConfig);
                auth = firebase.auth();
                
                // Set up auth state listener
                auth.onAuthStateChanged(user => {
                    if (user) {
                        userEmail.textContent = user.email;
                        userId.textContent = user.uid;
                        userInfo.classList.remove('hidden');
                        noUserInfo.classList.add('hidden');
                    } else {
                        userInfo.classList.add('hidden');
                        noUserInfo.classList.remove('hidden');
                    }
                });
                
                initStatus.textContent = 'Firebase initialized successfully!';
                initStatus.classList.add('success');
                initStatus.classList.remove('hidden', 'error');
                
                // Enable auth buttons
                registerBtn.disabled = false;
                loginBtn.disabled = false;
                
            } catch (error) {
                initStatus.textContent = `Error: ${error.message}`;
                initStatus.classList.add('error');
                initStatus.classList.remove('hidden', 'success');
            }
        });
        
        // Register function
        registerBtn.addEventListener('click', () => {
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            
            if (!email || !password) {
                authStatus.textContent = 'Please enter both email and password';
                authStatus.classList.add('error');
                authStatus.classList.remove('hidden', 'success');
                return;
            }
            
            auth.createUserWithEmailAndPassword(email, password)
                .then(userCredential => {
                    authStatus.textContent = 'User registered successfully!';
                    authStatus.classList.add('success');
                    authStatus.classList.remove('hidden', 'error');
                })
                .catch(error => {
                    authStatus.textContent = `Registration error: ${error.message}`;
                    authStatus.classList.add('error');
                    authStatus.classList.remove('hidden', 'success');
                });
        });
        
        // Login function
        loginBtn.addEventListener('click', () => {
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            
            if (!email || !password) {
                authStatus.textContent = 'Please enter both email and password';
                authStatus.classList.add('error');
                authStatus.classList.remove('hidden', 'success');
                return;
            }
            
            auth.signInWithEmailAndPassword(email, password)
                .then(userCredential => {
                    authStatus.textContent = 'Login successful!';
                    authStatus.classList.add('success');
                    authStatus.classList.remove('hidden', 'error');
                })
                .catch(error => {
                    authStatus.textContent = `Login error: ${error.message}`;
                    authStatus.classList.add('error');
                    authStatus.classList.remove('hidden', 'success');
                });
        });
        
        // Logout function
        logoutBtn.addEventListener('click', () => {
            auth.signOut()
                .then(() => {
                    authStatus.textContent = 'User logged out successfully';
                    authStatus.classList.add('success');
                    authStatus.classList.remove('hidden', 'error');
                })
                .catch(error => {
                    authStatus.textContent = `Logout error: ${error.message}`;
                    authStatus.classList.add('error');
                    authStatus.classList.remove('hidden', 'success');
                });
        });
        
        // Initially disable auth buttons until Firebase is initialized
        registerBtn.disabled = true;
        loginBtn.disabled = true;
    </script>
</body>
</html>