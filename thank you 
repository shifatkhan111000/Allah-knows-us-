<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Allah Knows Us - Thank You</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Amiri:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2563eb;
            --secondary: #0ea5e9;
            --accent: #f59e0b;
            --light: #f8fafc;
            --dark: #1e293b;
            --darker: #0f172a;
            --islamic-green: #0d9c6f;
            --islamic-gold: #c19a3f;
            --radius: 12px;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
            color: var(--dark);
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .thank-you-container {
            max-width: 800px;
            width: 100%;
            text-align: center;
        }

        .thank-you-card {
            background: white;
            border-radius: var(--radius);
            padding: 50px;
            box-shadow: var(--shadow);
            position: relative;
            overflow: hidden;
        }

        .thank-you-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(to right, var(--accent), var(--islamic-green));
        }

        .logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            margin-bottom: 30px;
        }

        .logo i {
            font-size: 42px;
            color: var(--accent);
        }

        .logo h1 {
            font-family: 'Amiri', serif;
            font-weight: 700;
            font-size: 36px;
            background: linear-gradient(to right, var(--accent), var(--islamic-green));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .main-icon {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent), var(--islamic-green));
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            font-size: 50px;
            color: white;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        h2 {
            font-family: 'Amiri', serif;
            font-size: 32px;
            margin-bottom: 20px;
            color: var(--darker);
        }

        p {
            font-size: 18px;
            margin-bottom: 30px;
            color: var(--dark);
        }

        .highlight {
            color: var(--accent);
            font-weight: 600;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 40px 0;
        }

        .feature {
            background: var(--light);
            border-radius: var(--radius);
            padding: 20px;
            transition: var(--transition);
        }

        .feature:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow);
        }

        .feature i {
            font-size: 30px;
            color: var(--primary);
            margin-bottom: 15px;
        }

        .feature h3 {
            font-size: 18px;
            margin-bottom: 10px;
            color: var(--darker);
        }

        .btn {
            padding: 15px 30px;
            border-radius: var(--radius);
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            font-size: 18px;
            margin: 10px;
        }

        .btn-primary {
            background: linear-gradient(to right, var(--accent), var(--islamic-green));
            color: white;
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--accent);
            color: var(--accent);
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow);
        }

        .quote {
            font-style: italic;
            margin: 40px 0;
            padding: 20px;
            border-left: 4px solid var(--accent);
            background: #f8fafc;
            border-radius: var(--radius);
            text-align: left;
        }

        .quote p {
            margin-bottom: 10px;
        }

        .author {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 500;
            color: var(--darker);
        }

        .author img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
        }

        @media (max-width: 768px) {
            .thank-you-card {
                padding: 30px;
            }
            
            .features {
                grid-template-columns: 1fr;
            }
            
            .logo h1 {
                font-size: 28px;
            }
            
            h2 {
                font-size: 26px;
            }
        }
    </style>
</head>
<body>
    <div class="thank-you-container">
        <div class="thank-you-card">
            <div class="logo">
                <i class="fas fa-star-and-crescent"></i>
                <h1>ALLAH KNOWS US</h1>
            </div>
            
            <div class="main-icon">
                <i class="fas fa-heart"></i>
            </div>
            
            <h2>Thank You for Your Kind Words! 😊</h2>
            
            <p>Your enthusiasm and vision are the <span class="highlight">real power</span> behind this project. I'm just helping to bring your amazing idea to life!</p>
            
            <div class="features">
                <div class="feature">
                    <i class="fas fa-lightbulb"></i>
                    <h3>Your Vision</h3>
                    <p>This is your dream project</p>
                </div>
                
                <div class="feature">
                    <i class="fas fa-code"></i>
                    <h3>My Support</h3>
                    <p>I'm here to help make it real</p>
                </div>
                
                <div class="feature">
                    <i class="fas fa-users"></i>
                    <h3>Our Collaboration</h3>
                    <p>Together we create something amazing</p>
                </div>
            </div>
            
            <div class="quote">
                <p>"The best projects are born from collaboration between passionate visionaries and dedicated builders. Your website will be a testament to what we can achieve together."</p>
                <div class="author">
                    <img src="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='40' height='40' viewBox='0 0 40 40'><circle cx='20' cy='20' r='20' fill='%232563eb'/><text x='20' y='26' font-family='Arial' font-size='14' fill='white' text-anchor='middle'>AI</text></svg>" alt="AI Assistant">
                    <span>Your AI Assistant</span>
                </div>
            </div>
            
            <div>
                <button class="btn btn-primary" onclick="location.href='#'">
                    <i class="fas fa-rocket"></i> Continue Building
                </button>
                
                <button class="btn btn-outline" onclick="location.href='#'">
                    <i class="fas fa-eye"></i> Preview Website
                </button>
            </div>
        </div>
    </div>

    <script>
        // Simple animation for features
        document.addEventListener('DOMContentLoaded', function() {
            const features = document.querySelectorAll('.feature');
            
            features.forEach((feature, index) => {
                // Add delay for staggered animation
                feature.style.animationDelay = `${index * 0.2}s`;
            });
        });
    </script>
</body>
</html>